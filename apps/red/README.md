# Red native configuration

Place the Firebase Android client file for `com.wecredit.multiapp.red` here as
`google-services.json`. The app config detects it automatically and remains usable for local and
OTA testing when the file is absent.
